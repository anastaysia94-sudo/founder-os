'use client';
import{useMemo,useState}from'react';import{demo,evidenceLabels,score,type Opportunity}from'@/lib/fdos';
export default function Home(){const[website,setWebsite]=useState('');const[facebook,setFacebook]=useState('');const[idea,setIdea]=useState('');const[scanned,setScanned]=useState(false);const ranked=useMemo(()=>[...demo].sort((a,b)=>score(b)-score(a)),[]);function runScan(e:React.FormEvent){e.preventDefault();setScanned(true)}return <main><header className="hero"><div><p className="eyebrow">SMARTPICKSHOP HOLDINGS // FOUNDER DYNASTY OS 10.0</p><h1>Build weird. Measure what works. <span>Keep the receipts.</span></h1><p className="lede">A standalone, non-WordPress Founder Command Center for turning messy business ideas into evidence-labeled experiments.</p></div><div className="status">EVIDENCE SYSTEM ONLINE</div></header><section className="grid metrics"><article><b>8</b><small>Evidence classes</small></article><article><b>1</b><small>Next best action</small></article><article><b>0</b><small>Fabricated results</small></article><article><b>∞</b><small>Experiments possible</small></article></section><section className="panel"><div className="panelHead"><div><p className="eyebrow">VALUE LEAK SCANNER // WEB EDITION</p><h2>What are you building?</h2></div><span className="tag">LOCAL DEMO · E8/E5</span></div><form onSubmit={runScan} className="form"><label>Website URL<input value={website} onChange={e=>setWebsite(e.target.value)} placeholder="https://yourbusiness.com"/></label><label>Facebook / business URL<input value={facebook} onChange={e=>setFacebook(e.target.value)} placeholder="Optional"/></label><label className="wide">Business idea / problem<textarea value={idea} onChange={e=>setIdea(e.target.value)} placeholder="Describe the business, customer, offer, or problem..."/></label><button>Run evidence-first scan</button></form>{scanned&&<p className="notice">This standalone preview does not claim to have fetched those URLs. The opportunities below are explicitly illustrative/hypothetical until a permissioned evidence adapter is connected.</p>}</section><section className="two"><div className="panel"><p className="eyebrow">OPPORTUNITY RANKING</p><h2>Next best actions</h2><div className="stack">{ranked.map((o:Opportunity)=><article className="op" key={o.id}><div><span className="evidence">{o.evidence} · {evidenceLabels[o.evidence]}</span><h3>{o.title}</h3><p>{o.observation}</p></div><strong>{score(o)}</strong></article>)}</div></div><div className="panel"><p className="eyebrow">VALUE SPRINT</p><h2>One measurable loop</h2><ol className="sprint"><li>Problem <span>Too much building, not enough external evidence.</span></li><li>Evidence <span>E5 hypothesis until tested.</span></li><li>Action <span>Put one founder through a complete scan → sprint loop.</span></li><li>KPI <span>Completed sprint with a recorded decision.</span></li><li>Decision <span>KEEP / REVISE / REVERT.</span></li></ol></div></section><section className="panel"><p className="eyebrow">EVIDENCE VAULT</p><h2>Truth has a type.</h2><div className="vault">{Object.entries(evidenceLabels).map(([k,v])=><div key={k}><b>{k}</b><span>{v}</span></div>)}</div></section><footer>Founder Dynasty OS · Standalone web track · No WordPress dependency in this app.</footer></main>}