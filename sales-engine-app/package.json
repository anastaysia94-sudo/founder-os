{
  "name": "same-day-customer-growth-pack-sales-os",
  "version": "1.1.0",
  "private": true,
  "description": "Same-Day Customer Growth Pack PWA with Supabase Auth and durable RLS-protected pipeline persistence",
  "scripts": {
    "start": "serve -s . -l $PORT",
    "check": "node --check server.js && node --check db.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "serve": "^14.2.4"
  }
}