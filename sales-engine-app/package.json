{
  "name": "same-day-customer-growth-pack-sales-os",
  "version": "1.0.0",
  "private": true,
  "description": "Same-Day Customer Growth Pack PWA with authenticated durable pipeline persistence",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  }
}